name: Radar
on: [push, pull_request]
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: auditware/radar-action@main
        with:
          path: 'programs/'
          fail-on: 'low'